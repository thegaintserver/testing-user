pipeline {
  agnet any
  stages {
    stage(build) {
      steps {
        sh 'echo "Building the code"'
      }
    }
    stage(deploy) {
      steps {
        sh 'echo "Deploying the code"'
      }
    }
  }
}
